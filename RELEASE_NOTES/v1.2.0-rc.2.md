## Improved

- Redesigned stats settings tab with radial ring dials, section icons, and a wider layout so module-type and storage breakdowns are easier to scan at a glance.
- Simplified Pi setup — the installer no longer prompts for a reporter token, and per-Pi hardware stats report automatically once a display is adopted. Existing installs clean up the old token on upgrade.
