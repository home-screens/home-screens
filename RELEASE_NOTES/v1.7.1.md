## Fixed

- Updating Home Screens no longer removes installed plugins, uploaded backgrounds, or local environment settings; updates now only replace the app's own code
- Plugin sign-in (like Strava or Garmin) now works when you open the editor by hostname or through a proxy; the connection link previously pointed at the wrong address (Fixes Strava Plugin)
