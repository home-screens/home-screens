/** Color token names that map to the real CSS variables. We keep them as
 * strings so the component can pass them into inline `style.stroke`. */
export type SemanticColor = 'success' | 'warning' | 'danger';
